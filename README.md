# nix-darwin for gmmyung

Personal `nix-darwin` + Home Manager setup for gmmyung's macOS workstation.

## Usage
```bash
git submodule update --init --recursive
sudo darwin-rebuild switch --flake .#gmmyung
```
